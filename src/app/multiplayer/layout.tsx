import type { ReactNode } from "react";

import "../styles/mini-games/multiplayer.css";

export default function MultiplayerLayout({ children }: Readonly<{ children: ReactNode }>) {
  return children;
}
